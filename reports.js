function reportData(finding, program = {}) {
  return { title: finding.title, severity: finding.severity, confidence: finding.confidence, cwe: finding.cwe || null, cvss: finding.cvss || null, asset: finding.asset, endpoint: finding.url || finding.asset, method: finding.method, parameter: finding.parameter, summary: finding.summary || finding.detail, impact: finding.impact, technicalDetails: finding.detail, reproduction: finding.reproduction || finding.steps || [], evidence: finding.evidence || [], validation: finding.status, remediation: finding.remediation, references: finding.references || [], timestamp: finding.createdAt, program: program.name || null }; }
function markdownReport(finding, program) { const data = reportData(finding, program); return [`# ${data.title}`, ``, `- Severity: ${data.severity}`, `- Confidence: ${data.confidence}`, `- Asset: ${data.asset}`, `- Endpoint: ${data.endpoint}`, `- Method: ${data.method}`, `- Parameter: ${data.parameter || 'None'}`, ``, `## Summary`, data.summary, ``, `## Impact`, data.impact, ``, `## Technical Details`, data.technicalDetails, ``, `## Reproduction`, ...data.reproduction.map((step, index) => `${index + 1}. ${step}`), ``, `## Evidence`, '```json', JSON.stringify(data.evidence, null, 2), '```', ``, `## Remediation`, data.remediation].join('\n'); }
function htmlReport(finding, program) { const escape = (value) => String(value ?? '').replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char])); return `<!doctype html><meta charset="utf-8"><title>${escape(finding.title)}</title><article><h1>${escape(finding.title)}</h1><p>${escape(finding.severity)} · ${escape(finding.confidence)} · ${escape(program.name || '')}</p><h2>Summary</h2><p>${escape(finding.summary || finding.detail)}</p><h2>Technical Details</h2><p>${escape(finding.detail)}</p><h2>Evidence</h2><pre>${escape(JSON.stringify(finding.evidence || [], null, 2))}</pre></article>`; }
module.exports = { reportData, markdownReport, htmlReport };